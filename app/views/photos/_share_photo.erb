<div class="photo" id="photo-<%= photo.id %>">
  <%= link_to image_tag(photo.image_url(:thumb_150).to_s), photo.image_url.to_s, data: { fancybox: (local_assigns[:feed_id] ? local_assigns[:feed_id] : 'photo'), caption: (render 'caption', photo: photo, from_album: false, alien: true) } %>
</div>
