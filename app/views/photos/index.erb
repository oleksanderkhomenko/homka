<% @photos.each do |photo| %>
  <div>
    <%= link_to image_tag(photo.image_url(:thumb).to_s), photo_path(photo) %>
    <span><%= link_to 'x', photo_path(photo), method: :delete %> </span>
  </div>
<% end %>
